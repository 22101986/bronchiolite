<div class="container-fluid" style="background-color: #6BA8DC;">
    <div class="container">
        <header class="py-3">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-12 d-flex justify-content-start">
                    {# <a href="{{ path('homepage') }}"> #}
                        <img class="img-fluid" src="{{ asset('assets/img/logo_3.png') }}" alt="Bronchiolite image">
                    {# </a> #}
                </div>
            </div>
        </header>
    </div>
</div>